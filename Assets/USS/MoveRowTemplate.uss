.move-row {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    height: 22px;
    padding-left: 8px;
    padding-right: 8px;
}

#name-label {
    flex-grow: 1;
}

#level-label {
    width: 80px;
}